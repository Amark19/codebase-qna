chromadb>=0.5.0
scikit-learn>=1.4.0
numpy>=1.26.0
anthropic>=0.40.0

# If using SentenceTransformerEmbeddingFunction instead of the offline
# TF-IDF fallback (recommended on machines with internet access):
# sentence-transformers>=2.7.0